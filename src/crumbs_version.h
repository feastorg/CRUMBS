#ifndef CRUMBS_VERSION_H
#define CRUMBS_VERSION_H

/**
 * @file crumbs_version.h
 * @brief Library version, updated at each release.
 */

#define CRUMBS_VERSION_MAJOR 0 /**< Major version. */
#define CRUMBS_VERSION_MINOR 13 /**< Minor version. */
#define CRUMBS_VERSION_PATCH 0 /**< Patch version. */
#define CRUMBS_VERSION_STRING "0.13.0" /**< Version as a string. */
#define CRUMBS_VERSION 1300 /**< major*10000 + minor*100 + patch, for `#if` comparisons. */

#endif // CRUMBS_VERSION_H
